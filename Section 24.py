"""PDF Template Maker
We get introduced to this concept by creating a single page before moving on to using data processing for template creation
"""

"""
for loop over csv row
    add page
    set font
    set color
    create cell
    add line
    add space using ln
    set csv row topic as header
    for loop over pg value using i=1 i<pg i++ or range for loops
        add page
        add space using ln
        add line
        set font
        set color
        create cell footer
        create cell page number
        set csv row topic as footer
"""
